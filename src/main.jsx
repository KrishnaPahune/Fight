import React from "react";
import ReactDOM from "react-dom/client";
import TrainingApp from "../krishna-training-app.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <TrainingApp />
  </React.StrictMode>
);
